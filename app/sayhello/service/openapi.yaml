# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: SayHello API
    description: SayHello 微服务 API 文档 - 一个简单的问候服务示例
    contact:
        name: Krathub Team
        url: https://github.com/horonlee/krathub
    license:
        name: MIT License
        url: https://github.com/horonlee/krathub/blob/main/LICENSE
    version: "1.0"
paths: {}
components:
    schemas:
        KratosError:
            type: object
            properties:
                code:
                    type: number
                    description: 错误码
                    format: int32
                message:
                    type: string
                    description: 错误消息
                reason:
                    type: string
                    description: 错误原因
            description: Kratos 错误响应
    responses:
        default:
            description: 默认 Kratos 错误响应
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/KratosError'
